# OpenClaw U 盘启动器项目二次开发说明

本文档用于项目交接、后续改版、外包协作和二次开发沟通。  
这里不展开源码实现细节，只说明项目是什么、有哪些模块、哪些文件重要、哪些地方适合改、哪些地方不要随便动。

## 1. 项目定位

本项目是一个面向 Windows 用户的 OpenClaw 便携启动器。

它的目标不是单纯启动一个程序，而是把 OpenClaw 本体、Node.js 运行环境、授权码激活、AI 生图、AI 视频、小广告视频分镜工作台、配置工具等能力打包成一个适合放进 U 盘运行的离线便携包。

最终用户拿到的应该是一个文件夹或安装包，核心入口是：

- `OpenClaw.exe`

用户不需要理解源码，也不需要自己安装 Python、Node.js 或 OpenClaw 依赖。

## 2. 当前主要能力

项目当前包含以下能力：

- OpenClaw 服务启动与停止
- 本地网页界面打开
- 在线授权码激活
- 授权后本地离线运行
- AI 生图配置与调用
- AI 视频配置与调用
- 小广告视频分镜工作台
- 三视图素材管理
- 首尾帧素材管理
- 九宫格候选图管理
- 分镜可编辑、可调整
- API 配置管理
- 飞书机器人配置入口
- OpenClaw 版本检查与更新
- 帮助文档入口
- 离线便携包清理与发布
- 授权码服务器后台 WebUI

## 3. 项目组成

项目可以按业务分成五块看。

### 3.1 桌面启动器

桌面启动器是用户看到的主界面，也就是 `OpenClaw.exe`。

它负责：

- 展示主界面
- 管理左侧功能导航
- 管理授权状态
- 启动 OpenClaw 服务
- 打开网页界面
- 进入生图、生视频、广告视频、配置等功能

后续 UI 改版主要围绕这一块进行。

### 3.2 OpenClaw 本体与运行环境

便携包里内置：

- Node.js
- OpenClaw 本体
- OpenClaw 依赖
- 启动脚本

这一块是 OpenClaw 能真正运行起来的基础。  
如果目标是做完整离线包，这部分必须保留。

### 3.3 授权系统

授权系统分为两部分：

- 客户端授权页：用户输入授权码完成激活
- 服务端授权后台：商家生成、查看、停用授权码

授权码适合用于售卖安装包，一份授权码可以绑定一台或多台设备，具体规则由服务端控制。

注意：授权服务器的管理员 Token、服务器密码、私钥等敏感信息不要写进文档、安装包或公开仓库。

### 3.4 小广告视频工作台

这是项目的重点增值功能。

它不是简单的视频生成按钮，而是为了保证小广告视频质量，提供一个可编辑的出片前工作区。

当前规划/已接入方向包括：

- 分镜列表
- 分镜文案
- 镜头说明
- 三视图素材
- 首帧素材
- 尾帧素材
- 九宫格候选图
- 图生视频素材准备
- 分镜顺序调整
- 素材保存到项目目录

后续如果要提高产品价值，可以优先继续完善这一块。

### 3.5 离线便携包与发布包

发布给客户的不是整个源码目录，而是 `release/OpenClaw-Portable` 或对应 zip 包。

售卖包应该尽量干净，只包含运行必需文件，不包含开发缓存、本机授权状态、临时文件。

## 4. 重要目录说明

### 4.1 根目录

| 路径 | 说明 | 二开建议 |
| --- | --- | --- |
| `launcher.py` | 程序入口 | 一般不要改 |
| `openclaw_launcher/` | 启动器主程序 | 主要二开目录 |
| `license_server/` | 授权码服务器 | 授权业务改这里 |
| `OpenClaw.spec` | 打包配置 | 改图标、打包入口时才动 |
| `logo.ico` | 主图标 | 建议统一使用这个 |
| `clean_portable_package.ps1` | 便携包清理脚本 | 发布前建议运行 |
| `dist/` | exe 打包输出 | 自动生成，可重建 |
| `release/` | 对外发布包 | 客户拿到的是这里的内容 |
| `build/` | 打包缓存 | 可删除 |
| `.cache/` | 下载/构建缓存 | 可删除 |
| `__pycache__/` | Python 缓存 | 可删除 |

### 4.2 启动器目录

| 路径 | 说明 | 适合改什么 |
| --- | --- | --- |
| `openclaw_launcher/app.py` | 主窗口和导航 | 页面入口、整体布局 |
| `openclaw_launcher/constants.py` | 统一配置 | 颜色、链接、默认模型、服务器地址 |
| `openclaw_launcher/pages/` | 功能页面 | 授权页、生图页、生视频页、分镜页 |
| `openclaw_launcher/dialogs/` | 弹窗 | API 配置、飞书配置等 |
| `openclaw_launcher/ui/` | 通用 UI 组件 | 按钮、输入框、导航样式 |
| `openclaw_launcher/services/` | 功能服务 | 启动 OpenClaw、检查更新、AI 请求 |
| `openclaw_launcher/license_manager.py` | 授权客户端 | 授权逻辑、许可证校验 |
| `openclaw_launcher/paths.py` | 路径管理 | 便携包目录规则 |

### 4.3 便携包目录

`release/OpenClaw-Portable` 是最终给客户使用的目录。

必须保留：

- `OpenClaw.exe`
- `node/`
- `node_modules/`
- `start.js`
- `package.json`
- `.npmrc`
- `logo.ico`
- `README-离线包.txt`
- `data/` 空目录

不建议带给客户：

- `data/install_id.txt`
- `data/license.json`
- `data/.openclaw/`
- `data/storyboards/`
- `*.map`
- `*.pdb`
- `package-lock.json`
- `.package-lock.json`
- 各类测试、示例、文档缓存
- `启动OpenClaw.bat`

原因：这些文件要么是本机状态，要么是开发调试文件，要么会增加包体积。

## 5. UI 二开说明

这个项目现在已经按模块拆开，UI 是可以继续替换的。

二开 UI 时建议按下面顺序改：

1. 先改 `constants.py` 里的颜色、字体、链接、默认文案
2. 再改 `ui/` 里的通用组件样式
3. 再改 `pages/` 里的具体页面
4. 最后再动 `app.py` 的整体布局和导航

不要一开始就大改所有文件，否则后续不好维护。

### 5.1 当前 UI 风格方向

当前版本偏现代工具型界面：

- 左侧功能导航
- 右侧主工作区
- 浅色主界面
- 暖灰背景
- 绿色作为主要操作色
- 琥珀色作为焦点/提醒色
- `logo.ico` 作为统一品牌图标

如果后续继续美化，建议保持“专业工具”方向，不要做成太花哨的营销页。

### 5.2 授权页注意事项

授权页是新用户第一眼看到的重要页面。

它需要重点保证：

- 授权码输入框明显
- 激活按钮明显
- 错误提示清楚
- 成功状态清楚
- 安装 ID 可见但不要喧宾夺主

授权码输入框不建议再做成和背景接近的颜色，否则用户不知道在哪里输入。

## 6. 授权系统说明

### 6.1 授权方式

当前授权方式是在线授权：

1. 用户打开启动器
2. 输入授权码
3. 启动器请求授权服务器
4. 服务器验证授权码
5. 激活成功后，本地保存许可证
6. 后续可离线运行

### 6.2 授权码适合的售卖方式

可以做到：

- 一份安装包对应一个授权码
- 一个授权码只允许激活一台设备
- 一个授权码允许激活多台设备
- 到期时间控制
- 版本控制
- 功能控制
- 后台停用授权码

这些适合用于卖离线包、卖 U 盘、卖行业定制版。

### 6.3 授权后台

授权后台用于商家自己管理授权码。

当前后台能力：

- 生成授权码
- 查看授权码列表
- 查看客户名称
- 查看版本
- 查看到期时间
- 查看激活次数
- 启用/停用授权码

后台入口和管理员 Token 属于敏感信息，不建议写进对外文档。

### 6.4 授权相关注意事项

不要把以下内容放进安装包：

- 授权服务器管理员 Token
- 服务器登录密码
- 授权服务器私钥
- 已激活的 `license.json`
- 已生成的 `install_id.txt`

尤其是 `install_id.txt`，如果跟安装包一起卖出去，可能导致不同客户共享同一个安装身份。

## 7. 小广告视频工作台二开方向

这是后续最值得继续投入的模块。

建议按产品价值从高到低分阶段做：

### 7.1 第一阶段：让分镜更好用

- 每个分镜支持标题
- 每个分镜支持镜头描述
- 每个分镜支持口播文案
- 每个分镜支持产品卖点
- 每个分镜支持时长
- 每个分镜支持拖动排序
- 每个分镜支持复制
- 每个分镜支持删除

### 7.2 第二阶段：让素材更清楚

- 三视图区域更明显
- 首帧/尾帧区域更明显
- 九宫格候选图支持选择主图
- 每个镜头可以绑定自己的素材
- 支持素材缺失提醒

### 7.3 第三阶段：让出片流程更完整

- 一键生成分镜脚本
- 一键生成镜头提示词
- 一键生成候选图
- 一键把选中的首尾帧送入视频生成
- 自动保存项目
- 导出项目包
- 导出给客户看的分镜 PDF 或图片

### 7.4 第四阶段：商业化增强

- 视频模板
- 行业模板
- 品类模板
- 品牌色配置
- 标题字幕预设
- 口播风格预设
- 批量生成广告版本
- 客户项目管理

## 8. AI 生图与 AI 视频说明

### 8.1 AI 生图

AI 生图模块主要用于：

- 产品图生成
- 广告图生成
- 分镜素材生成
- 候选图生成

后续可以增加：

- 模板提示词
- 历史记录
- 批量生成
- 风格预设
- 自动保存到分镜项目

### 8.2 AI 视频

AI 视频模块主要用于：

- 文生视频
- 图生视频
- 分镜视频片段生成

后续可以增加：

- 和分镜页更深度联动
- 每个分镜独立生成视频
- 失败重试
- 视频片段管理
- 视频预览和替换

## 9. OpenClaw 本体说明

OpenClaw 本体是整个启动器真正调用的 AI 助手运行核心。

便携包里需要包含：

- Node.js
- OpenClaw 依赖
- OpenClaw 启动脚本

启动器本身不替代 OpenClaw，它是 OpenClaw 的图形化入口、配置管理器和商业化外壳。

如果未来要做“完整安装包”，仍然需要包含 OpenClaw 本体和运行依赖。  
如果不包含，用户电脑就必须自己联网安装依赖，这会降低售卖包体验。

## 10. 发布包清理规则

发布前建议运行：

- `clean_portable_package.ps1`

它会清理便携包中不该卖给客户的内容。

清理目标包括：

- 本机安装 ID
- 本机许可证
- 本机 OpenClaw 状态
- 分镜草稿
- 调试文件
- source map
- PDB
- lock 缓存
- 非必要文档和示例
- 多余 bat 快捷脚本
- 非 Windows x64 原生依赖

发布前一定要检查：

- `data/install_id.txt` 不存在
- `data/license.json` 不存在
- zip 里有 `OpenClaw.exe`
- zip 里有 `node/node.exe`
- zip 里有 `node_modules/openclaw`
- zip 里有 `start.js`

## 11. 常见二开需求对应位置

| 需求 | 建议修改位置 |
| --- | --- |
| 换 logo | 根目录 `logo.ico`，并重新打包 |
| 换主色调 | `openclaw_launcher/constants.py` |
| 改左侧导航 | `openclaw_launcher/app.py` |
| 改授权页 | `openclaw_launcher/pages/license_page.py` |
| 改生图页 | `openclaw_launcher/pages/image_page.py` |
| 改视频页 | `openclaw_launcher/pages/video_page.py` |
| 改分镜页 | `openclaw_launcher/pages/storyboard_page.py` |
| 改 API 配置弹窗 | `openclaw_launcher/dialogs/api_config.py` |
| 改飞书配置弹窗 | `openclaw_launcher/dialogs/feishu_config.py` |
| 改授权服务器地址 | `openclaw_launcher/constants.py` |
| 改授权码规则 | `license_server/` |
| 改发布包清理规则 | `clean_portable_package.ps1` |
| 改打包图标/入口 | `OpenClaw.spec` |

## 12. 不建议随便改的地方

以下内容会影响运行稳定性，不建议没有确认就改：

- `start.js`
- `node/`
- `node_modules/openclaw/`
- `openclaw_launcher/paths.py`
- 授权公钥
- 授权服务器私钥
- `OpenClaw.spec` 里的入口配置
- 便携包目录结构

如果必须改，改完后一定要重新打包并完整测试。

## 13. 发布前检查清单

每次准备发给客户前，建议按这个清单检查：

- 能打开 `OpenClaw.exe`
- 未授权时会进入授权页
- 授权码输入框明显可见
- 输入有效授权码能激活
- 错误授权码有清楚提示
- 激活后能进入主功能
- 能启动 OpenClaw 服务
- 能打开网页界面
- AI 生图页面能打开
- AI 视频页面能打开
- 小广告视频工作台能打开
- `data/install_id.txt` 没有被打进安装包
- `data/license.json` 没有被打进安装包
- `启动OpenClaw.bat` 没有被打进安装包
- zip 包能正常解压
- 解压后目录结构完整
- 在另一台干净电脑或虚拟机上至少启动一次

## 14. 适合后续商业化增强的功能

建议优先级：

1. 授权后台绑定订单号
2. 授权后台增加备注和客户手机号
3. 授权码批量导出 Excel
4. 授权码到期提醒
5. 启动器自动检查新版本
6. 一键更新启动器
7. 小广告视频模板库
8. 分镜项目管理
9. 一键导出客户交付文件
10. 独立安装器
11. 域名 HTTPS 授权后台
12. 多版本套餐授权

## 15. 对外协作建议

如果找别人二开，建议只给对方以下信息：

- 这份说明文档
- 当前源码目录
- 目标功能清单
- UI 参考图
- 测试授权码
- 测试用授权服务器地址

不建议直接给：

- 服务器 root 密码
- 授权后台管理员 Token
- 授权服务器私钥
- 正式客户授权码
- 已激活的客户数据

## 16. 当前项目状态

当前项目已经完成：

- 启动器模块化重构
- UI 初步现代化
- 授权码输入页
- 在线授权客户端
- 授权码服务器
- 授权码后台 WebUI
- 便携离线包
- 发布包清理脚本
- 小广告视频工作台基础能力

后续重点建议：

- 继续完善小广告视频工作台
- 优化 UI 细节
- 增强授权后台
- 做正式安装器
- 域名 HTTPS 化
- 建立标准发布流程

