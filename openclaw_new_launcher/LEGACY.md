# Legacy Launcher Directory

`openclaw_new_launcher` is kept for historical reference only.

The current release, CI, documentation, and macOS/Windows packaging source of truth is:

```text
openclaw_ui_integration/
```

Do not start new release work from this directory unless a migration task explicitly requires comparing legacy behavior.
